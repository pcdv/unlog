const initialState = {
  maxLines: 15000,
  maxChars: 10 * 1000 * 1000,
}

export default function settings(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}